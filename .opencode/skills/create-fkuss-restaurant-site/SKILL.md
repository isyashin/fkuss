---
name: create-fkuss-restaurant-site
description: "Создать изолированный сайт ресторана на Fkuss по ссылке Яндекс.Еды, меню и проверенным открытым источникам: собрать content, подтвердить контакты, проверить, показать preview и при явном запросе развернуть. Использовать для новых ресторанов и полного перенаполнения существующего сайта; не использовать для разработки кода платформы."
license: MIT
---

# Создать сайт ресторана Fkuss

Собери готовый сайт ресторана, не смешивая данные клиента с кодом Fkuss.
Для реального ресторана меняй только `sites/<slug>/content/**`. Не редактируй
`template/src`, `template/scripts`, `platform` и эксплуатационные скрипты.

## Обязательные правила

- Цены и состав блюд бери из входных материалов или Яндекс.Еды. Не выдумывай их.
- Контакты и часы подтверждай по официальному сайту/соцсетям либо у владельца.
- Не публикуй заглушки, неподтверждённые контакты и несуществующие изображения.
- Не копируй случайные фотографии из поиска. Используй материалы владельца,
  официальные ресурсы ресторана или изображения из его карточки Яндекс.Еды;
  сомнения по правам перечисли до публикации.
- Секреты живут только в `secrets/sites/<slug>.env`; в content допустимы лишь
  имена переменных вида `*Ref`.
- Seed, деплой и другие изменения внешнего состояния выполняй только после
  сводки и подтверждения пользователя.

## Маршрут работы

1. Определи slug, целевой домен и каталог `sites/<slug>/content`. Не используй
   `template/content` для клиента, если пользователь явно не просит изменить
   демонстрационный ресторан.
2. Для ссылки Яндекс.Еды выполни инжест по процессу из
   [workflow.md](references/workflow.md). Для файлов или текста собери те же
   JSON вручную по `docs/CONTENT-SCHEMA.md`.
3. Если нужны адрес, телефон, часы, официальный сайт или соцсети, проведи
   исследование по [research-and-sources.md](references/research-and-sources.md)
   и сохрани `source-report.json` рядом с content-файлами.
4. Заполни все обязательные JSON и изображения. Выбирай тему по формату
   заведения; тексты пиши заново по подтверждённым фактам.
5. Покажи сводку: источники, категории/блюда/фото, найденные контакты,
   выбранная тема, домен, пробелы и сомнения. Дождись подтверждения.
6. Запусти readiness/seed, preview и проверку по
   [acceptance.md](references/acceptance.md). Исправляй только content.
7. Разворачивай сайт лишь по явному запросу. После деплоя проверь URL,
   заказ без реального списания, бронь, мобильную вёрстку и админ-вход.

Если Яндекс изменил API или включил защиту, остановись и попроси меню файлом.
Не обходи антибот и не используй чужую авторизованную сессию без разрешения.
