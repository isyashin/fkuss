# PLAN.md — план проекта

**AI-наполняемый шаблон сайта ресторана.** Нейроагент получает меню + фото
(или ссылку на Яндекс.Еду) и собирает готовый сайт на субдомене
`название.нашдомен.ru` или на собственном домене клиента.

Статус этапов: `[ ]` не начат, `[~]` в работе, `[x]` готов.

---

## Зафиксированные решения

### Продукт
- Шаблон content-driven: агент правит только `content/` (JSON + картинки),
  код шаблона не трогает.
- Витрина: меню, карточки блюд, корзина, оформление заказа
  (доставка/самовывоз), галерея, акции, бронирование.
- Админка: CRUD меню/галереи/акций, WYSIWYG текстов (Tiptap),
  настройки доставки/каналов/оплаты/лояльности/брони.
- Админка — НЕ конструктор сайта уровня Tilda, а редактор контента.

### Каналы приёма заказов
- Заказ без онлайн-оплаты → структурированное сообщение (состав, количество,
  суммы, контакты, адрес, комментарий) в: Telegram (Bot API), MAX (Bot API),
  email (SMTP). Каналы включаются/настраиваются в админке.
- WhatsApp — кнопка `wa.me/<номер>?text=<состав заказа>` (клиент сам
  отправляет). Платный Business API — вне MVP.

### Оплата
- Абстракция `PaymentProvider` (createPayment / handleWebhook / getStatus).
- Первая реализация — ЮKassa; ключи — в настройках конкретного ресторана.
- Mock-провайдер для тестов. Fallback всегда: «оплата при получении».

### Антибот
- По умолчанию (бесплатно): honeypot-поле, rate limit по IP и телефону,
  серверный пересчёт суммы заказа по БД.
- Яндекс SmartCaptcha — опционально (платная, тарификация за успешные
  валидации), ключи в настройках ресторана.
- Авторизованные гости заказывают без капчи.

### Авторизация и лояльность
- Вход: email + одноразовый 6-значный код (10 мин, 3 попытки, rate limit
  1/мин на email, 5/час с IP). Сессия httpOnly cookie 30 дней.
- Телефон обязателен в заказе (контакт), но не используется для входа.
- Личный кабинет: история заказов, повтор заказа, адреса, баланс бонусов,
  мои брони.
- Лояльность: кэшбэк бонусами (дефолт 5%), оплата бонусами до 20% заказа
  (дефолт). Оба параметра — в админке. Ledger `BonusTransaction`
  (начисление/списание/сторно), не просто поле баланса.
- Антифрод: бонусы не начисляются на сумму, оплаченную бонусами;
  при отмене заказа начисленные бонусы сторнируются.

### Бронирование
- Слоты (шаг и длительность — в админке), форма: дата/время/гости/имя/телефон/
  комментарий. Статусы: новая → подтверждена / отклонена / отменена гостем.
- Подтверждение кнопкой в админке. Уведомления — в те же каналы, что заказы.
- Схема зала со столиками — фаза 2, не MVP.

### PWA («как приложение»)
- `manifest.json` генерируется из `content/restaurant.json` (название, цвета),
  иконки из логотипа (sharp: 192/512 + apple-touch-icon 180).
- Кнопка «Установить приложение»: Android — системный диалог
  (beforeinstallprompt), iOS — всплывающая инструкция (Safari не даёт
  программной установки).
- Push-уведомления — вне MVP.

### Мультитенантность и домены
- На ресторан: свой контейнер приложения, своя БД + свой пользователь в общем
  PostgreSQL, свой volume для images, свой `.env` в `secrets/sites/`.
- Лимиты CPU/RAM на контейнер в compose.
- Субдомены: Caddy выпускает per-domain сертификаты автоматически
  (HTTP/TLS-ALPN). Wildcard — только если DNS-провайдер даст API (DNS-01).
- Собственный домен клиента: A-запись на IP сервера + on-demand TLS в Caddy
  с обязательным ask-эндпоинтом (allowlist). Переезд субдомен → свой домен
  с 301-редиректом.
- Canonical домен в `settings.json` — используется в письмах, мессенджерах,
  PWA, Open Graph.

### Дизайн (по изучению Noma, Hawksmoor, Sugarfish, Peter Luger, White Rabbit)
- 3 ручных пресета на токенах (`content/theme.json`): `warm` (трактир/
  стейкхаус), `minimal` (кофейня/суши), `elegant` (тёмный, fine dining).
- Токены: accent-цвет, шрифт заголовков (сериф) / текста (гротеск),
  скругления, светлая/тёмная.
- Структура главной: hero (одно большое фото) → о нас → меню/популярное →
  акции → [награды/пресса — опционально] → галерея → контакты с картой.
- Sticky CTA «Заказать» / «Забронировать» на всех экранах.
- Без каруселей, параллаксов, тяжёлых эффектов.
- Тон текстов под пресет (правило в навыке site-builder).

### Mobile requirements (обязательные критерии приёмки)
- Вёрстка от 360px, десктоп — адаптация наверх.
- Зоны касания ≥ 44×44px.
- Sticky нижняя панель корзины с суммой на странице меню.
- Корзина и оформление — bottom sheet, не отдельные страницы.
- Карточка блюда — полноэкранная модалка, фото крупно, «Добавить» снизу.
- Категории меню — горизонтальные прилипающие табы со scroll-spy.
- Кликабельные `tel:` и кнопка маршрута (Яндекс.Карты).
- Правильные клавиатуры (tel/email), маска телефона, минимум полей.
- Фото: AVIF/WebP, responsive, lazy-load. LCP < 2,5 с на 4G.
- SSR для контента, JS только для корзины/форм. Шрифты `font-display: swap`.
- E2E-сценарии прогоняются в мобильной эмуляции.

### Инфраструктура разработки
- Разработка на виртуалке Ubuntu Server (SSH, у пользователя на машине),
  идентичной боевому окружению.
- Секреты: `secrets/` в папке проекта, в git не попадают, на сервере права 600.
- OpenCode: `instructions` только AGENTS.md; MCP — context7 (всегда) и
  playwright (с этапа 3); Postgres MCP не используем (bash + psql/prisma).

### Платформа владельца (SaaS-слой) — сайты продаются
- Проект — коммерческий: сайты ресторанов продаются по подписке.
- **Три кабинета (модель UX, зафиксирована):**
  1. **Админка платформы** (`platform/admin`) — владелец видит все сайты,
     метрики, балансы, управляет ими.
  2. **Админка ресторана** (сайт `/admin`) — настройки сайта, заказы,
     **и подписка/баланс/оплата** (раздел «Подписка», данные тянутся
     с платформы по X-Site-Key: `/api/sites/me`, `/api/billing/topup`).
  3. **ЛК гостя** (сайт `/account`) — история заказов, бонусы, брони.
- Кабинет владельца на платформе (`/cabinet`) остаётся как дублирующий вход.
- **Платформа** — отдельное приложение `platform/` в этом же репозитории,
  свой поддомен (`admin.нашдомен.ru`), своя БД. Код платформы и шаблона
  разделён; платформа не имеет доступа к данным гостей ресторанов.
- **Биллинг**: у каждого сайта баланс (₽) и тариф. Списание ежедневное
  (тариф/30). Пополнение: оплата картой/СБП через ЮKassa платформы
  (переиспользуем PaymentProvider) + генерация PDF-счёта для юрлиц
  (реквизиты — в настройках платформы).
- **Уведомления о балансе**: за 7 / 3 / 1 дней до исчерпания — владельцу
  сайта на email и/или в Telegram (канал задаётся в кабинете платформы).
- **Исчерпание баланса**: льготный период (по умолчанию 3 дня, сайт работает),
  затем приостановка — заглушка «сайт временно недоступен», данные НЕ
  удаляются. Реактивация автоматически после пополнения.
- **Метрики**: каждый сайт шлёт на API платформы (по site key) агрегаты:
  заказы (число, сумма), брони, посещаемость. Посещаемость — свой лёгкий
  счётчик просмотров (без внешней аналитики); Яндекс.Метрика — опционально,
  по желанию клиента.
- **Админка владельца платформы**: список сайтов (состояние, баланс,
  тариф, версия образа, домен), метрики по каждому сайту, история счетов
  и оплат, ручные операции (начислить/скорректировать баланс, приостановить,
  реактивировать).
- **Экспорт сайта**: кнопка → архив (content/ + дамп БД + images) → ссылка
  на скачивание. Доступно владельцу платформы и владельцу сайта.
- Подробности: `docs/BILLING.md`.

---

## Этапы

### Этап 0 — Документация и конфигурация [x]
- [x] AGENTS.md, docs/ (PLAN, ARCHITECTURE, CONTENT-SCHEMA, DEPLOY, CHANNELS,
      PAYMENTS), .gitignore, opencode.json
- [x] Кастомные агенты (site-builder, reviewer), команды (/new-site, /deploy,
      /check-site), заготовки навыков

### Этап 1 — Скелет шаблона [x]
- [x] Next.js 16 (App Router) + Tailwind 4 (shadcn/ui — по мере надобности,
      кастомные компоненты на токенах уже работают)
- [x] Дизайн-токены + 3 пресета (warm/minimal/elegant), тема из theme.json
- [x] PWA-база: manifest.webmanifest генерируется из content/restaurant.json
- [x] Prisma 7 + PostgreSQL (важно: Prisma 7 — generator `prisma-client`,
      подключение через `@prisma/adapter-pg`, URL в `prisma.config.ts`)
- [x] Dockerfile (standalone multi-stage) + docker-compose (app + postgres),
      infra/Caddyfile.example
- [x] Контентная раздача картинок: /content-asset/* из content/
- [x] Приёмка: `npm run build` проходит локально; на виртуалке (Ubuntu 26.04,
      Docker 29) `docker compose up -d` поднимается, главная отдаётся с
      тестовым контентом (HTTP 200, манифест с именем ресторана)

### Этап 2 — Модель данных + seed [x]
- [x] Prisma-схема: Category, Dish, Modifier, Order, OrderItem, Customer,
      AuthCode, Address, BonusTransaction, Reservation, Promo, GalleryImage,
      Page, Settings
- [x] zod-схемы content/*.json (`src/lib/content-schema.ts`, единый источник
      правды; docs/CONTENT-SCHEMA.md синхронизирован по смыслу)
- [x] scripts/seed.ts: content/ → БД, транзакция, идемпотентно; заказы и
      клиентов не трогает (OrderItem.dishId без FK — меню можно пересидировать)
- [x] Тестовый контент: 4 категории, 11 блюд, 1 акция, 2 страницы
- [x] Приёмка: seed наполняет БД на ВМ (проверено psql: 11/4/1/2/3),
      невалидный JSON отклонён с перечнем ошибок

### Этап 3 — Витрина [x]
- [x] Главная (блоки из theme.json), меню (табы категорий со scroll,
      полноэкранная карточка блюда с модификаторами), корзина
      (zustand + localStorage), оформление заказа (bottom sheet)
- [ ] Галерея — блок отрисуется после появления фото (этап 8/9)
- [x] Страницы /p/[slug], контакты с Яндекс.Картой (виджет по адресу)
- [x] Антибот: honeypot + rate limit (5 заказов/10 мин) + серверный
      пересчёт суммы по БД (TDD: 24 теста pricing — позиции, модификаторы,
      зоны доставки, freeFrom, минималка, бонусы)
- [x] Кнопка «Установить приложение» (Android prompt / iOS инструкция)
- [x] Бронирование (форма + API + антибот) — задел этапа 7
- [x] Приёмка: E2E Playwright «меню → корзина → заказ» и «бронь»
      зелёные в мобильной (WebKit/iPhone 13) и десктопной эмуляции;
      honeypot 400, rate limit 429 подтверждены запросами
- [ ] Playwright MCP включить при необходимости (E2E идёт через
      @playwright/test в bash — MCP опционален)

### Этап 4 — Уведомления [x] (код), [~] (живой прогон — нужны токены)
- [x] notify.ts: Telegram Bot API, MAX (platform-api.max.ru), email (SMTP);
      формат сообщения: состав/количество/суммы/контакты/адрес/комментарий
- [x] Уведомления о заказах и бронях; падение канала не роняет заказ
      (Promise.allSettled)
- [x] Кнопка «Дублировать заказ в WhatsApp» (wa.me с текстом заказа)
      на экране успеха
- [ ] Приёмка живого канала: ждёт токенов (TELEGRAM_BOT_TOKEN + chat_id
      тестовой группы) — см. docs/CHANNELS.md

### Этап 5 — Оплата [x] (код), [~] (живой sandbox — нужны ключи ЮKassa)
- [x] PaymentProvider (createPayment/parseWebhook/verifyWebhook) + mock + ЮKassa
- [x] Webhook /api/payments/webhook/[provider]: проверка по allowlist IP ЮKassa,
      идемпотентность, paid → заказ «принят» + уведомление в каналы
- [x] Онлайн-оплата в заказе: paymentMethod=online → confirmationUrl;
      уведомление о заказе уходит после оплаты
- [x] TDD: 8 тестов (payload ЮKassa, парсинг webhook, выбор провайдера)
- [ ] Живой прогон sandbox ЮKassa — ждёт ключей (этап 11)

### Этап 6 — Авторизация + личный кабинет + бонусы [x]
- [x] Email + одноразовый 6-значный код (10 мин, 3 попытки; rate limit
      1/мин на email, 5/час с IP); сессия httpOnly cookie 30 дней;
      dev-режим без SMTP: код в ответе при AUTH_DEV_CODE=1
- [x] Кабинет: баланс бонусов, история заказов со статусами, мои брони, выход
- [x] BonusTransaction ledger (TDD, интеграционные тесты на dev-БД):
      начисление при «выполнен», идемпотентность, сторно при отмене,
      возврат потраченных, заказ без клиента не падает — 5 тестов
- [x] Приёмка: E2E вход по коду в мобильной и десктопной эмуляции;
      нюанс Secure-cookie на http решён флагом INSECURE_HTTP (только тесты)

### Этап 7 — Бронирование [x] + часы работы (полная версия)
- [x] Форма (дата/время/гости/контакты), слоты из настроек, honeypot,
      rate limit, статусы new/confirmed/rejected/cancelled
- [x] Уведомления о брони в те же каналы
- [x] E2E: «форма брони отправляется»
- [x] **Часы работы (полная):** расписание по дням недели + исключения по
      датам (праздники), ночной режим через полночь (TDD, 18 тестов
      lib/hours.ts); fallback из workHours для старого контента
- [x] Бронь только в слотах рабочих часов: UI грузит слоты
      (/api/booking/slots), серверная валидация (E2E: 03:00 → 400)
- [x] Админка «Ресторан»: основные данные, редактор расписания по дням,
      исключения; workHours-строки для контактов выводятся из расписания
- [x] «Ресторан закрыт» — плашка на странице меню вне часов работы

### Этап 8 — Админка [x]
- [x] Вход ресторатора: ADMIN_PASSWORD + подписанная HMAC-кука (7 дней),
      route group (protected) с guard
- [x] Заказы: список, смена статусов по переходам; «выполнен» → начисление
      кэшбэка, «отменён» → сторно/возврат (через loyalty ledger)
- [x] Брони: список, подтверждение/отклонение
- [x] Меню: CRUD блюд и категорий, «в наличии/нет», загрузка фото
      (sharp → WebP ≤1600px в content/images/dishes/)
- [x] Акции, галерея (загрузка/удаление), страницы (текстовый редактор;
      полноценный Tiptap — отложено, сейчас textarea)
- [x] Настройки: тема (пресет/accent), доставка (зоны/минималка), лояльность,
      каналы, бронирование
- [x] Приёмка: E2E «вход → заказы → смена статуса», guard без пароля → логин;
      правка меню видна на витрине без пересборки (force-dynamic + revalidate)

### Этап 9 — Навык restaurant-site-builder [x]
- [x] Спайк подтвердил: меню Яндекс.Еды доступно без логина через
      `/api/v2/menu/retrieve/<slug>` + инфо через `/api/v2/catalog/<slug>`
- [x] scripts/ingest-yandex-eda.ts: URL → content/ (restaurant, menu с zod-
      валидацией, фото → WebP 800px, логотип, дефолты settings/theme)
- [x] Прогон на Чайхана Бухара: 12 категорий, 132 блюда, 109 фото —
      из реальной ссылки `https://eda.yandex.ru/r/chajxana_buxara?placeSlug=…`
- [x] Демо мультитенанта: второй сайт поднят на той же ВМ (общий образ,
      своя БД buxara, volume с контентом, порт 3001) — меню отдаётся,
      фото раздаются
- [x] Отчёт пробелов: телефон/email/соцсети Яндекс.Еда не отдаёт —
      заполняются вручную (предупреждение скрипта)
- [ ] SKILL.md синхронизировать с реальным скриптом (упомянуть
      scripts/ingest-yandex-eda.ts)

### Этап 10 — E2E полный цикл [x]
- [x] 14 E2E Playwright (mobile WebKit/iPhone 13 + desktop Chromium):
      заказ без оплаты, бронь, вход по коду, неверный код, админ-вход,
      guard, смена статуса, полный цикл лояльности (заказ → «выполнен» →
      кэшбэк 24 ₽ в кабинете)
- [x] Юнит/интеграционные vitest: 24 pricing + 8 payments + 5 loyalty
- [x] Приёмка: все зелёные; устройство-сайт на ВМ проверен запросами
      (HTTP 200, заказ через API)

### Этап 11 — Боевой деплой [~]
- [x] Docker 29 + Caddy 2.6 на ВМ (Ubuntu 26.04)
- [x] scripts/deploy.sh (реестр портов registry.json, создание БД, compose
      сайта, генерация ADMIN_PASSWORD), backup.sh (дампы БД + content,
      хранит 14), export-site.sh (архив content+SQL для клиента)
- [x] Caddy: маршрутизация по Host (проверено curl'ами: buxara→3001,
      u-mamy→3000, platform→3100)
- [x] Cron на хосте: метрики (*/6ч), billing-daily (04:30), backup (05:00);
      джобы — защищённые HTTP-роуты внутри приложений (X-Cron-Secret)
- [x] Сквозной цикл на ВМ: сайт шлёт метрики по docker-сети → платформа
      принимает и хранит
- [x] Мультитенант на ВМ: общий образ resto-template, изолированные БД,
      volume content на сайт, лимиты CPU/RAM
- [ ] Перенос на прод-VPS с публичным IP: DNS wildcard, реальный TLS,
      SMTP (DKIM/SPF), ufw/fail2ban — **сервер получен: 217.65.3.123 (fkuss.ru),
      SSH по ключу работает, A-запись настроена; осталось: wildcard-DNS
      (действие владельца), Docker/Caddy/SMTP**
- [ ] Ask-эндпоинт TLS подключён к Caddy (готов: /api/tls-ask платформы)
- Примечание: разработка и приёмка идут на ВМ в LAN; механика TLS
  описана в docs/DEPLOY.md и протестирована локально через Host-заголовки.

### Этап 12 — Платформа владельца: биллинг и метрики [x] (MVP)
- [x] Приложение `platform/` (Next.js, своя БД platform, порт 3100)
- [x] Модель: Site/Tariff/BalanceTransaction/Invoice/Payment/MetricSnapshot/
      OwnerAccount/OwnerAuthCode
- [x] API метрик /api/metrics (X-Site-Key, zod, upsert по дате;
      неверный ключ → 403)
- [x] Шаблон: счётчик просмотров (/api/pv + beacon, бот-фильтр) и
      scripts/report-metrics.ts (агрегаты заказов/броней → платформа)
- [x] Биллинг (TDD, 15 тестов): dailyCharge в копейках, daysLeft, переходы
      active→grace→suspended, пороги уведомлений 7/3/1 без повторов
- [x] scripts/billing-daily.ts: списание, переход состояния, уведомление
      владельцу (проверено: buxara active→grace при минусе)
- [x] Пополнение: ЮKassa платформы (провайдер из settings) + mock;
      webhook → topup в ledger + реактивация grace/suspended → active
- [x] Админка владельца платформы: список сайтов (баланс, «хватит на N дней»,
      состояние), карточка сайта (метрики 14 дн., ledger, тариф,
      приостановка/реактивация, ручные операции с комментарием)
- [x] Кабинет владельца сайта: вход по email-коду (коды в БД), баланс,
      пополнение, счета, метрики, запрос экспорта (флаг → cron на хосте)
- [x] E2E платформы (mobile+desktop, serial — общий баланс): вход →
      пополнение 1000 ₽ → баланс изменился
- [ ] PDF-счета для юрлиц (этап 12.1)
- [ ] ask-эндпоинт подключён в прод-Caddy (с этапом 11 на прода)
- Найдено и исправлено по ходу: in-memory коды владельца заменены на БД
  (рассинхрон между процессами), Secure-cookie на http в тестах (INSECURE_HTTP),
  флаки E2E от параллельного доступа к общему балансу (workers: 1)

### Ревью безопасности (после этапа 12) — блокеры исправлены
- [x] Списание бонусов/привязка заказа — только из серверной сессии
      (было: по email из тела запроса — угон чужих бонусов)
- [x] Mock-оплата платформы отключена в production без флага
      ALLOW_MOCK_PAYMENT (было: накрутка баланса по автоинкремент-URL)
- [x] Код входа владельца не пишется в логи
- [x] Per-site пользователь PostgreSQL с генерируемым паролем в deploy.sh
      (было: общий resto:resto на все тенанты)
- [x] Ежедневное списание биллинга идемпотентно (charge раз в сутки)
- [x] Из замечаний: timingSafeEqual в платформе, crypto.randomInt для кодов,
      Math.round в ordersSum, path.relative в content-asset, откат/пометка
      заказа при падении createPayment, rate limit на логины админок и
      запросы кодов платформы, запрет входа в production без заданного
      ADMIN_PASSWORD/PLATFORM_ADMIN_PASSWORD

### Открытые задачи (бэклог)
- Мониторинг ресурсов сервера в админке платформы: нагрузка общая и по
  каждому сайту с историей (когда расширять). Текст issue:
  `docs/issues/monitoring-host-metrics.md`
- Тарифная модель: фикс ₽/мес на сайт? От количества заказов? Пробный период?
  Цены и градации — решает владелец, в шаблоне: гибкая таблица тарифов.
