# PLAN.md — план проекта

**AI-наполняемый шаблон сайта ресторана.** Нейроагент получает меню + фото
(или ссылку на Яндекс.Еду) и собирает готовый сайт на субдомене
`название.нашдомен.ru` или на собственном домене клиента.

Статус этапов: `[ ]` не начат, `[~]` в работе, `[x]` готов.

---

## Зафиксированные решения

### Продукт
- Шаблон content-driven: агент правит только `content/` (JSON + картинки),
  код шаблона не трогает.
- Витрина: меню, карточки блюд, корзина, оформление заказа
  (доставка/самовывоз), галерея, акции, бронирование.
- Админка: CRUD меню/галереи/акций, WYSIWYG текстов (Tiptap),
  настройки доставки/каналов/оплаты/лояльности/брони.
- Админка — НЕ конструктор сайта уровня Tilda, а редактор контента.

### Каналы приёма заказов
- Заказ без онлайн-оплаты → структурированное сообщение (состав, количество,
  суммы, контакты, адрес, комментарий) в: Telegram (Bot API), MAX (Bot API),
  email (SMTP). Каналы включаются/настраиваются в админке.
- WhatsApp — кнопка `wa.me/<номер>?text=<состав заказа>` (клиент сам
  отправляет). Платный Business API — вне MVP.

### Оплата
- Абстракция `PaymentProvider` (createPayment / handleWebhook / getStatus).
- Первая реализация — ЮKassa; ключи — в настройках конкретного ресторана.
- Mock-провайдер для тестов. Fallback всегда: «оплата при получении».

### Антибот
- По умолчанию (бесплатно): honeypot-поле, rate limit по IP и телефону,
  серверный пересчёт суммы заказа по БД.
- Яндекс SmartCaptcha — опционально (платная, тарификация за успешные
  валидации), ключи в настройках ресторана.
- Авторизованные гости заказывают без капчи.

### Авторизация и лояльность
- Вход: email + одноразовый 6-значный код (10 мин, 3 попытки, rate limit
  1/мин на email, 5/час с IP). Сессия httpOnly cookie 30 дней.
- Телефон обязателен в заказе (контакт), но не используется для входа.
- Личный кабинет: история заказов, повтор заказа, адреса, баланс бонусов,
  мои брони.
- Лояльность: кэшбэк бонусами (дефолт 5%), оплата бонусами до 20% заказа
  (дефолт). Оба параметра — в админке. Ledger `BonusTransaction`
  (начисление/списание/сторно), не просто поле баланса.
- Антифрод: бонусы не начисляются на сумму, оплаченную бонусами;
  при отмене заказа начисленные бонусы сторнируются.

### Бронирование
- Слоты (шаг и длительность — в админке), форма: дата/время/гости/имя/телефон/
  комментарий. Статусы: новая → подтверждена / отклонена / отменена гостем.
- Подтверждение кнопкой в админке. Уведомления — в те же каналы, что заказы.
- Схема зала со столиками — фаза 2, не MVP.

### PWA («как приложение»)
- `manifest.json` генерируется из `content/restaurant.json` (название, цвета),
  иконки из логотипа (sharp: 192/512 + apple-touch-icon 180).
- Кнопка «Установить приложение»: Android — системный диалог
  (beforeinstallprompt), iOS — всплывающая инструкция (Safari не даёт
  программной установки).
- Push-уведомления — вне MVP.

### Мультитенантность и домены
- На ресторан: свой контейнер приложения, своя БД + свой пользователь в общем
  PostgreSQL, свой volume для images, свой `.env` в `secrets/sites/`.
- Лимиты CPU/RAM на контейнер в compose.
- Субдомены: Caddy выпускает per-domain сертификаты автоматически
  (HTTP/TLS-ALPN). Wildcard — только если DNS-провайдер даст API (DNS-01).
- Собственный домен клиента: A-запись на IP сервера + on-demand TLS в Caddy
  с обязательным ask-эндпоинтом (allowlist). Переезд субдомен → свой домен
  с 301-редиректом.
- Canonical домен в `settings.json` — используется в письмах, мессенджерах,
  PWA, Open Graph.

### Дизайн (по изучению Noma, Hawksmoor, Sugarfish, Peter Luger, White Rabbit)
- 3 ручных пресета на токенах (`content/theme.json`): `warm` (трактир/
  стейкхаус), `minimal` (кофейня/суши), `elegant` (тёмный, fine dining).
- Токены: accent-цвет, шрифт заголовков (сериф) / текста (гротеск),
  скругления, светлая/тёмная.
- Структура главной: hero (одно большое фото) → о нас → меню/популярное →
  акции → [награды/пресса — опционально] → галерея → контакты с картой.
- Sticky CTA «Заказать» / «Забронировать» на всех экранах.
- Без каруселей, параллаксов, тяжёлых эффектов.
- Тон текстов под пресет (правило в навыке site-builder).

### Mobile requirements (обязательные критерии приёмки)
- Вёрстка от 360px, десктоп — адаптация наверх.
- Зоны касания ≥ 44×44px.
- Sticky нижняя панель корзины с суммой на странице меню.
- Корзина и оформление — bottom sheet, не отдельные страницы.
- Карточка блюда — полноэкранная модалка, фото крупно, «Добавить» снизу.
- Категории меню — горизонтальные прилипающие табы со scroll-spy.
- Кликабельные `tel:` и кнопка маршрута (Яндекс.Карты).
- Правильные клавиатуры (tel/email), маска телефона, минимум полей.
- Фото: AVIF/WebP, responsive, lazy-load. LCP < 2,5 с на 4G.
- SSR для контента, JS только для корзины/форм. Шрифты `font-display: swap`.
- E2E-сценарии прогоняются в мобильной эмуляции.

### Инфраструктура разработки
- Разработка на виртуалке Ubuntu Server (SSH, у пользователя на машине),
  идентичной боевому окружению.
- Секреты: `secrets/` в папке проекта, в git не попадают, на сервере права 600.
- OpenCode: `instructions` только AGENTS.md; MCP — context7 (всегда) и
  playwright (с этапа 3); Postgres MCP не используем (bash + psql/prisma).

### Платформа владельца (SaaS-слой) — сайты продаются
- Проект — коммерческий: сайты ресторанов продаются по подписке.
- **Три кабинета (модель UX, зафиксирована):**
  1. **Админка платформы** (`platform/admin`) — владелец видит все сайты,
     метрики, балансы, управляет ими.
  2. **Админка ресторана** (сайт `/admin`) — настройки сайта, заказы,
     **и подписка/баланс/оплата** (раздел «Подписка», данные тянутся
     с платформы по X-Site-Key: `/api/sites/me`, `/api/billing/topup`).
  3. **ЛК гостя** (сайт `/account`) — история заказов, бонусы, брони.
- Кабинет владельца на платформе (`/cabinet`) остаётся как дублирующий вход.
- **Платформа** — отдельное приложение `platform/` в этом же репозитории,
  свой поддомен (`admin.нашдомен.ru`), своя БД. Код платформы и шаблона
  разделён; платформа не имеет доступа к данным гостей ресторанов.
- **Биллинг**: у каждого сайта баланс (₽) и тариф. Списание ежедневное
  (тариф/30). Пополнение: оплата картой/СБП через ЮKassa платформы
  (переиспользуем PaymentProvider) + генерация PDF-счёта для юрлиц
  (реквизиты — в настройках платформы).
- **Уведомления о балансе**: за 7 / 3 / 1 дней до исчерпания — владельцу
  сайта на email и/или в Telegram (канал задаётся в кабинете платформы).
- **Исчерпание баланса**: льготный период (по умолчанию 3 дня, сайт работает),
  затем приостановка — заглушка «сайт временно недоступен», данные НЕ
  удаляются. Реактивация автоматически после пополнения.
- **Метрики**: каждый сайт шлёт на API платформы (по site key) агрегаты:
  заказы (число, сумма), брони, посещаемость. Посещаемость — свой лёгкий
  счётчик просмотров (без внешней аналитики); Яндекс.Метрика — опционально,
  по желанию клиента.
- **Админка владельца платформы**: список сайтов (состояние, баланс,
  тариф, версия образа, домен), метрики по каждому сайту, история счетов
  и оплат, ручные операции (начислить/скорректировать баланс, приостановить,
  реактивировать).
- **Экспорт сайта**: кнопка → архив (content/ + дамп БД + images) → ссылка
  на скачивание. Доступно владельцу платформы и владельцу сайта.
- Подробности: `docs/BILLING.md`.

---

## Этапы

### Этап 0 — Документация и конфигурация [x]
- [x] AGENTS.md, docs/ (PLAN, ARCHITECTURE, CONTENT-SCHEMA, DEPLOY, CHANNELS,
      PAYMENTS), .gitignore, opencode.json
- [x] Кастомные агенты (site-builder, reviewer), команды (/new-site, /deploy,
      /check-site), заготовки навыков

### Этап 1 — Скелет шаблона [x]
- [x] Next.js 16 (App Router) + Tailwind 4 (shadcn/ui — по мере надобности,
      кастомные компоненты на токенах уже работают)
- [x] Дизайн-токены + 3 пресета (warm/minimal/elegant), тема из theme.json
- [x] PWA-база: manifest.webmanifest генерируется из content/restaurant.json
- [x] Prisma 7 + PostgreSQL (важно: Prisma 7 — generator `prisma-client`,
      подключение через `@prisma/adapter-pg`, URL в `prisma.config.ts`)
- [x] Dockerfile (standalone multi-stage) + docker-compose (app + postgres),
      infra/Caddyfile.example
- [x] Контентная раздача картинок: /content-asset/* из content/
- [x] Приёмка: `npm run build` проходит локально; на виртуалке (Ubuntu 26.04,
      Docker 29) `docker compose up -d` поднимается, главная отдаётся с
      тестовым контентом (HTTP 200, манифест с именем ресторана)

### Этап 2 — Модель данных + seed [x]
- [x] Prisma-схема: Category, Dish, Modifier, Order, OrderItem, Customer,
      AuthCode, Address, BonusTransaction, Reservation, Promo, GalleryImage,
      Page, Settings
- [x] zod-схемы content/*.json (`src/lib/content-schema.ts`, единый источник
      правды; docs/CONTENT-SCHEMA.md синхронизирован по смыслу)
- [x] scripts/seed.ts: content/ → БД, транзакция, идемпотентно; заказы и
      клиентов не трогает (OrderItem.dishId без FK — меню можно пересидировать);
      источник контента централизован через `CONTENT_DIR` (по умолчанию
      `content/`, для tenant-а задаётся явно)
- [x] Перед seed выполняется content-readiness: проверка обязательных JSON,
      draft-заглушек и путей изображений внутри `CONTENT_DIR`; неготовый
      контент отклоняется до записи в БД
- [x] Тестовый контент: 4 категории, 11 блюд, 1 акция, 2 страницы
- [x] Приёмка: seed наполняет БД на ВМ (проверено psql: 11/4/1/2/3),
      невалидный JSON отклонён с перечнем ошибок

### Этап 3 — Витрина [x]
- [x] Главная (блоки из theme.json), меню (табы категорий со scroll,
      полноэкранная карточка блюда с модификаторами), корзина
      (zustand + localStorage), оформление заказа (bottom sheet)
- [ ] Галерея — блок отрисуется после появления фото (этап 8/9)
- [x] Страницы /p/[slug], контакты с Яндекс.Картой (виджет по адресу)
- [x] Антибот: honeypot + rate limit (5 заказов/10 мин) + серверный
      пересчёт суммы по БД (TDD: 24 теста pricing — позиции, модификаторы,
      зоны доставки, freeFrom, минималка, бонусы)
- [x] Кнопка «Установить приложение» (Android prompt / iOS инструкция)
- [x] Бронирование (форма + API + антибот) — задел этапа 7
- [x] Приёмка: E2E Playwright «меню → корзина → заказ» и «бронь»
      зелёные в мобильной (WebKit/iPhone 13) и десктопной эмуляции;
      honeypot 400, rate limit 429 подтверждены запросами
- [ ] Playwright MCP включить при необходимости (E2E идёт через
      @playwright/test в bash — MCP опционален)

### Этап 4 — Уведомления [x] (код), [~] (живой прогон — нужны токены)
- [x] notify.ts: Telegram Bot API, MAX (platform-api.max.ru), email (SMTP);
      формат сообщения: состав/количество/суммы/контакты/адрес/комментарий
- [x] Уведомления о заказах и бронях; падение канала не роняет заказ
      (Promise.allSettled)
- [x] Кнопка «Дублировать заказ в WhatsApp» (wa.me с текстом заказа)
      на экране успеха
- [ ] Приёмка живого канала: ждёт токенов (TELEGRAM_BOT_TOKEN + chat_id
      тестовой группы) — см. docs/CHANNELS.md

### Этап 5 — Оплата [x] (код), [~] (живой sandbox — нужны ключи ЮKassa)
- [x] PaymentProvider (createPayment/parseWebhook/verifyWebhook) + mock + ЮKassa
- [x] Webhook /api/payments/webhook/[provider]: проверка по allowlist IP ЮKassa,
      идемпотентность, paid → заказ «принят» + уведомление в каналы
- [x] Онлайн-оплата в заказе: paymentMethod=online → confirmationUrl;
      уведомление о заказе уходит после оплаты
- [x] TDD: 8 тестов (payload ЮKassa, парсинг webhook, выбор провайдера)
- [ ] Живой прогон sandbox ЮKassa — ждёт ключей (этап 11)

### Этап 6 — Авторизация + личный кабинет + бонусы [x]
- [x] Email + одноразовый 6-значный код (10 мин, 3 попытки; rate limit
      1/мин на email, 5/час с IP); сессия httpOnly cookie 30 дней;
      dev-режим без SMTP: код в ответе при AUTH_DEV_CODE=1
- [x] Кабинет: баланс бонусов, история заказов со статусами, мои брони, выход
- [x] BonusTransaction ledger (TDD, интеграционные тесты на dev-БД):
      начисление при финале «Доставлен»/«Выдан», идемпотентность, сторно при отмене,
      возврат потраченных, заказ без клиента не падает — 5 тестов
- [x] Приёмка: E2E вход по коду в мобильной и десктопной эмуляции;
      нюанс Secure-cookie на http решён флагом INSECURE_HTTP (только тесты)

### Этап 7 — Бронирование [x] + часы работы (полная версия)
- [x] Форма (дата/время/гости/контакты), слоты из настроек, honeypot,
      rate limit, статусы new/confirmed/rejected/cancelled
- [x] Уведомления о брони в те же каналы
- [x] E2E: «форма брони отправляется»
- [x] **Часы работы (полная):** расписание по дням недели + исключения по
      датам (праздники), ночной режим через полночь (TDD, 18 тестов
      lib/hours.ts); fallback из workHours для старого контента
- [x] Бронь только в слотах рабочих часов: UI грузит слоты
      (/api/booking/slots), серверная валидация (E2E: 03:00 → 400)
- [x] Админка «Ресторан»: основные данные, редактор расписания по дням,
      исключения; workHours-строки для контактов выводятся из расписания
- [x] «Ресторан закрыт» — плашка на странице меню вне часов работы

### Этап 8 — Админка [x]
- [x] Вход ресторатора: личные учётные записи с ролями, серверной сессией
      (7 дней) и защитой страниц и действий
- [x] Заказы: список, смена статусов по переходам; «Доставлен»/«Выдан» → начисление
      кэшбэка, «отменён» → сторно/возврат (через loyalty ledger)
- [x] Брони: список, подтверждение/отклонение
- [x] Меню: CRUD блюд и категорий, «в наличии/нет», загрузка фото
      (sharp → WebP ≤1600px в content/images/dishes/)
- [x] Акции, галерея (загрузка/удаление), страницы (текстовый редактор;
      полноценный Tiptap — отложено, сейчас textarea)
- [x] Настройки: тема (пресет/accent), доставка (зоны/минималка), лояльность,
      каналы, бронирование
- [x] Приёмка: E2E «вход → заказы → смена статуса», guard без пароля → логин;
      правка меню видна на витрине без пересборки (force-dynamic + revalidate)

### Этап 9 — Навык restaurant-site-builder [x]
- [x] Спайк подтвердил: меню Яндекс.Еды доступно без логина через
      `/api/v2/menu/retrieve/<slug>` + инфо через `/api/v2/catalog/<slug>`
- [x] scripts/ingest-yandex-eda.ts: URL → content/ (restaurant, menu с zod-
      валидацией, фото → WebP 800px, логотип, дефолты settings/theme)
- [x] Живой прогон импорта на Чайхана Бухара: 12 категорий, 130 блюд,
      130 фото —
      из реальной ссылки `https://eda.yandex.ru/r/chajxana_buxara?placeSlug=…`
- [x] Демо мультитенанта: второй сайт поднят на той же ВМ (общий образ,
      своя БД buxara, volume с контентом, порт 3001) — меню отдаётся,
      фото раздаются
- [x] Отчёт источников и пробелов (`source-report.json`): телефон/email/
      соцсети Яндекс.Еда не отдаёт; контакты оставлены `DRAFT` из-за
      неоднозначности результатов поиска и требуют подтверждения владельца
- [x] Навык «Создать сайт ресторана Fkuss» синхронизирован с реальным
      процессом: `CONTENT_DIR`, readiness перед seed, provenance и
      `source-report.json`

### Этап 10 — E2E полный цикл [x]
- [x] 82 E2E Playwright: 78 шаблона (360 px Chromium, мобильный WebKit,
      desktop Chromium) + 4 платформы (mobile/desktop). Покрыты заказ, бронь,
      доставка, модификаторы, лояльность, PWA, фон, банкеты, админка,
      печатные материалы, responsive/overflow и битые content assets.
- [x] Юнит/интеграционные Vitest: 135 шаблона + 35 платформы.
- [x] Приёмка 2026-09-22: на dev-ВМ зелёные production build, миграции,
      seed и 82 E2E; текущие 170 Vitest подтверждены обязательным CI.
      Рабочие БД при проверках не менялись.

### Этап 11 — Боевой деплой [~]
- [x] Docker 29 + Caddy 2.6 на ВМ (Ubuntu 26.04)
- [x] scripts/deploy.sh (реестр портов registry.json, создание БД, compose
      сайта, генерация ADMIN_PASSWORD), backup.sh (дампы БД + content,
      хранит 14), export-site.sh (архив content+SQL для клиента)
- [x] Caddy: маршрутизация по Host (проверено curl'ами: buxara→3001,
      u-mamy→3000, platform→3100)
- [x] Cron на хосте: метрики (*/6ч), billing-daily (04:30), backup (05:00);
      джобы — защищённые HTTP-роуты внутри приложений (X-Cron-Secret)
- [x] Сквозной цикл на ВМ: сайт шлёт метрики по docker-сети → платформа
      принимает и хранит
- [x] Мультитенант на ВМ: общий образ resto-template, изолированные БД,
      volume content на сайт, лимиты CPU/RAM
- [x] Демо-compose использует явный `resto-template:latest`: `update.sh`
      перезапускает демо и tenant-сайты на одном и том же свежем образе
- [x] Перенос на прод-VPS: **ВЫПОЛНЕНО 2026-09-16**, обновлено 2026-09-22 (релиз v0.1)
      - Сервер подготовлен: LVM 15→30 ГБ, обновления, ufw (22/80/443),
        fail2ban, unattended-upgrades
      - HTTPS: buxara.fkuss.ru, admin.fkuss.ru, ochag-grill.fkuss.ru, fkuss.ru —
        реальные сертификаты (Caddy auto-TLS, wildcard `*.fkuss.ru` в DNS)
      - **2026-09-22: прод пересобран под релиз v0.1 (main)**: удалены демо
        u-mamy и старый ochag-стаб; перенесены с dev-ВМ Бухара (dump БД +
        content, baseline миграций 0001/0002) и Очаг Grill (deploy.sh → порт
        3002, seed по SSH-туннелю, регистрация в платформе + topup 3000 ₽,
        sync ok, TLS авто); миграции платформы baselined (alters dedupeKey/
        exportReadyPath + resolve 0001–0003); admin-пароли сайтов временные
        (заменить перед публичным запуском); cron через run-site-job.sh
        (секреты вне crontab); бэкап перед чисткой: backups/2026-09-22_15-59
      - **2026-09-23: третий сайт — «Чайхана Алия» (Коломна)**: aliya.fkuss.ru,
        порт 3003, инжест из Еды (placeSlug chajxana_aliya, 9 категорий,
        69 блюд), sync ok, тариф Стандарт + topup 3000 ₽, админ-пароль по
        решению владельца; конвейер создания сайта прогнан в третий раз
        без ручных правок (deploy.sh → seed по туннелю → платформа → Caddy).
      - Сайт «Бухара»: 132 блюда с фото, заказ по HTTPS работает,
        метрики → платформа по docker-сети
      - Биллинг: джоб списал (active→grace), топап через API + mock-оплата,
        баланс 2 900 ₽, active, 29 дней
      - Cron на проде: метрики */6ч и sync-menu */15 (все сайты, через
        run-site-job.sh), биллинг 04:30, бэкап 05:00, process-exports */5
      - Найденный баг (исправлен): .gitignore `sites/` глотал
        `platform/src/app/api/sites/` — якорь `/sites/` (88c3e0a)
      - Пароли админок сгенерированы на сервере, в git не хранятся
      - Осталось на потом: SMTP (коды входа смотреть в логах платформы),
        ЮKassa, Telegram/MAX токены
- [ ] Ask-эндпоинт TLS подключён к Caddy (готов: /api/tls-ask платформы)
- Примечание: разработка и приёмка идут на ВМ в LAN; механика TLS
  описана в docs/DEPLOY.md и протестирована локально через Host-заголовки.

### Этап 12 — Платформа владельца: биллинг и метрики [x] (MVP)
- [x] Приложение `platform/` (Next.js, своя БД platform, порт 3100)
- [x] Модель: Site/Tariff/BalanceTransaction/Invoice/Payment/MetricSnapshot/
      OwnerAccount/OwnerAuthCode
- [x] API метрик /api/metrics (X-Site-Key, zod, upsert по дате;
      неверный ключ → 403)
- [x] Шаблон: счётчик просмотров (/api/pv + beacon, бот-фильтр) и
      scripts/report-metrics.ts (агрегаты заказов/броней → платформа)
- [x] Биллинг (TDD, 15 тестов): dailyCharge в копейках, daysLeft, переходы
      active→grace→suspended, пороги уведомлений 7/3/1 без повторов
- [x] scripts/billing-daily.ts: списание, переход состояния, уведомление
      владельцу (проверено: buxara active→grace при минусе)
- [x] Пополнение: ЮKassa платформы (провайдер из settings) + mock;
      webhook → topup в ledger + реактивация grace/suspended → active
- [x] Админка владельца платформы: список сайтов (баланс, «хватит на N дней»,
      состояние), карточка сайта (метрики 14 дн., ledger, тариф,
      приостановка/реактивация, ручные операции с комментарием)
- [x] Кабинет владельца сайта: вход по email-коду (коды в БД), баланс,
      пополнение, счета, метрики, запрос экспорта (флаг → cron на хосте)
- [x] E2E платформы (mobile+desktop, serial — общий баланс): вход →
      пополнение 1000 ₽ → баланс изменился
- [ ] PDF-счета для юрлиц (этап 12.1)
- [ ] ask-эндпоинт подключён в прод-Caddy (с этапом 11 на прода)
- Найдено и исправлено по ходу: in-memory коды владельца заменены на БД
  (рассинхрон между процессами), Secure-cookie на http в тестах (INSECURE_HTTP),
  флаки E2E от параллельного доступа к общему балансу (workers: 1)

### Ревью безопасности (после этапа 12) — блокеры исправлены
- [x] Списание бонусов/привязка заказа — только из серверной сессии
      (было: по email из тела запроса — угон чужих бонусов)
- [x] Mock-оплата платформы отключена в production без флага
      ALLOW_MOCK_PAYMENT (было: накрутка баланса по автоинкремент-URL)
- [x] Код входа владельца не пишется в логи
- [x] Per-site пользователь PostgreSQL с генерируемым паролем в deploy.sh
      (было: общий resto:resto на все тенанты)
- [x] Ежедневное списание биллинга идемпотентно (charge раз в сутки)
- [x] Из замечаний: timingSafeEqual в платформе, crypto.randomInt для кодов,
      Math.round в ordersSum, path.relative в content-asset, откат/пометка
      заказа при падении createPayment, rate limit на логины админок и
      запросы кодов платформы, запрет входа в production платформы без
      `PLATFORM_ADMIN_PASSWORD`; админка ресторана требует bootstrap владельца

### Открытые задачи (бэклог)
- Расширение витрины и админки: синхронизация стоп-листа и цен с Яндекс.Едой,
  группы модификаторов, интервалы доставки, новая главная, банкеты, фон и
  PWA-иконки. Полное ТЗ, порядок реализации и критерии приёмки:
  `docs/TZ-RESTAURANT-ENHANCEMENTS.md`. Независимый план приёмки:
  `docs/TEST-PLAN-RESTAURANT-ENHANCEMENTS.md`.
- Мониторинг ресурсов сервера в админке платформы: нагрузка общая и по
  каждому сайту с историей (когда расширять). Текст issue:
  `docs/issues/monitoring-host-metrics.md`
- Печатные материалы (визитки/магниты с QR) — отдельная задача Codex,
  ветка `codex/print-materials` (Этап ПМ-1). Не трогать из main-линии.

### Волны исправлений приёмки 20.09 (BUG-002…027, журнал: `тестировщик/журнал.md`)
- [x] Волна A — деньги и идемпотентность (BUG-002, 003, 015)
- [x] Волна B — sync, доставка, бронь, оплата и бонусы в UI (BUG-004, 006–014)
- [x] Волна C — безопасность, валидация, контент, гостевой функционал
      (BUG-021, 022, 027, 018, 019)
- [x] Волна D ч.1 — линт, уязвимости, права volume, suspended-заглушка,
      экспорт (BUG-024, 025, 016, 017)
- [x] Волна D ч.2 — race-hardening: `billing-ledger` (topup/notify claim),
      HMAC-сессии владельца, `bonus-lock` и capacity брони через advisory
      lock, серверный gate `isSyncDue`, `admin-validation`, atomic
      backup/export, параметризованный SQL, stage-валидация Caddy,
      `install/run-site-job.sh`; QA-инфра: `npm test` в обоих пакетах,
      `test-with-db.sh`, `.github/workflows/test.yml`, проект `mobile-360` +
      `responsive.spec`; новый модуль `platform/src/lib/export-access.ts`
      (имена архивов, владение, сброс готового архива при повторном запросе)
- [x] Волна D ч.3 — повторный чистый CI после исправления E2E-окружения:
      реальные проекты WebKit, раздельные IP для rate limit, тестовые владельцы
      платформы, SQL teardown без импорта generated Prisma; экспорт: безопасная
      выдача, TTL 24 часа, lock воркера и автоматическая установка cron.
      GitHub Actions `35609347912`: template и platform полностью зелёные.
- [ ] Остатки вне кода: BUG-001 (железо dev-ВМ), тарифная модель.
- [x] Решение по BUG-020 принято владельцем: полный offline-кеш не нужен;
      установленный сайт требует сеть, на уже открытой странице показывается
      плашка «Нет подключения» без демонстрации устаревших цен и стоп-листа.
- Интеграционные тесты (loyalty 5 + sync 9) — только с тестовой БД
  (`template/scripts/test-with-db.sh`, нужен Docker); E2E — серийно
  (`workers: 1`)

### Производительность (2026-09-16)
- [x] Медленная загрузка картинок меню («десятки секунд») исправлена:
      причина — runtime-оптимизатор Next.js (`/_next/image`) с sharp на каждый
      первый просмотр каждого блюда на контейнере с лимитом 1 CPU.
      Фикс: прегенерированные размеры (400px карточка + 800px модалка) при
      инжесте/загрузке админки/бэкфилле в seed; витрина отдаёт WebP напрямую
      (plain img + lazy), content-asset с Cache-Control 1 день.
      Замер после: 20 карточек параллельно ~1,1 с (было десятки секунд
      на холодном кэше).

### Счётчик визитов (2026-09-23)
- [x] Баг: proxy счётчика просмотров (`src/proxy.ts`) не регистрировался в
      сборке Next 16.3.5 (именованный экспорт) — визиты не считались с волны C.
      Фикс: default-экспорт; в Next 16 регистрация видна в
      `.next/server/functions-config-manifest.json` (middleware-manifest
      унаследованный и остаётся пустым — не ориентир).
- [x] Страж регрессии: `template/scripts/check-proxy-registered.mjs` +
      шаг в CI после `next build` (падение, если `/_middleware` не в сборке).
- [x] Приёмка: rebuild dev+prod, боевой заход → MetricsCounter +1 →
      report-metrics → «визиты» в админке платформы > 0.

### Багфиксы витрины 2026-09-25
- [x] **Часы работы:** плашка «закрыто» считалась по UTC сервера → по утрам
      МСК врала. `lib/hours.ts` — `nowInTimeZone(tz)` (Intl, откат к
      Europe/Moscow); главная считает открытие по таймзоне ресторана из
      настроек. TDD: 5 тестов (переход даты, ночная смена, bad tz).
- [x] **Фон:** применялся глобально (html body) — просачивался в /admin;
      на мобилке скрывался дефолтным disableOnMobile=true. Фикс: proxy
      отдаёт x-pathname, root layout не отдаёт фон для /admin; дефолт
      disableOnMobile → false (чекбокс в админке сохранён).
- [x] **Состав блюда:** отдельное поле `Dish.composition` (миграция
      0003_dish_composition): zod/seed, редактор блюда — два поля
      («Состав» и «Описание»), карточка на витрине — блок «Состав:».
      Инжест/sync: форвард-маппинг «Ингредиенты» Еды (публичный API пока
      не отдаёт — состав заполняется вручную; sync не затирает ручное
      значение при пустом источнике; 2 интеграционных теста).

## ТЗ: синхронизация и развитие витрины (текущая работа)

Полное ТЗ: `docs/TZ-RESTAURANT-ENHANCEMENTS.md`. Приёмка: отдельный агент
по `docs/TEST-PLAN-RESTAURANT-ENHANCEMENTS.md` (ворота A–F). Пуш в git
разрешён после каждого зелёного этапа. Деплой на прод — после приёмки
тестировщиком.

### Этап ТЗ-1 — Модели и миграция [x]
- [x] Dish: source, externalId (unique), yandexPrice, manualPrice, priceMode,
      coefficientPercent, manualAvailable, yandexAvailable, lastSyncedAt
- [x] ModifierGroup (min/max/позиция) + Modifier: groupId, externalId,
      yandexPrice, manualPrice; Category.externalId
- [x] Order: deliveryMode, deliveryDate, слоты, tz, название варианта
      (desiredTime сохранено для совместимости); DeliveryOption
- [x] Settings(zod): sync (enabled/placeSlug/interval), pricing
      (globalMode/globalPercent)
- [x] Миграция на копии buxara_mig: 132 блюда, 2 заказа, снимки цен целы,
      дефолты безопасные; применена на resto и buxara
- [x] Откат: дамп БД + схема из git-тега до миграции (prisma db push)
- [x] zod-схемы (groups в menu, sync/pricing в settings), seed (группы,
      externalId из dish-<num>/mod-<num> → source=yandex), тестовый
      контент с группой «Соусы» (min 0, max 3)
- [x] CONTENT-SCHEMA.md обновлён; 55 vitest + 16 E2E зелёные

### Этап ТЗ-2 — Синхронизация Яндекс.Еды [x]
- [x] Изолированный клиент `lib/yandex-eda/client.ts` (timeout 10 с,
      нормализация, min/max групп из API: required/minSelected/maxSelected)
- [x] syncMenu: upsert только по externalId (переименование/перенос категории
      обновляют ту же запись), пропавшее → yandexAvailable=false без удаления,
      ручные (source=manual) не трогаются, группы/опции синкаются по externalId
- [x] Состояние в Settings(syncState): running/lastAttempt/lastSuccess/
      lastStatus/lastError; атомарная блокировка параллельных запусков
      (updateMany по JSON-флагу), retry ×2 с backoff
- [x] Job `/api/jobs/sync-menu` (X-Cron-Secret, skip при выключенной sync)
- [x] Админка «Синхронизация»: включение, placeSlug, периодичность,
      состояние, кнопка «Синхронизировать сейчас», ошибка
- [x] 9 интеграционных тестов (fixture, без сети): первая sync, стоп,
      возврат, переименование, пропавшая позиция, ручное блюдо, ошибка API,
      параллельный запуск, группы
- [x] Пойман и исправлен побочный баг: cleanup sync-тестов затирал модификаторы
      общей dev-БД (область очистки ограничена)
### Этап ТЗ-3 — Цены [x]
- [x] Резолвер `lib/order/price-resolver.ts`: режимы inherit/yandex/manual/
      coefficient глобально и на блюдо, индивидуальный приоритет,
      математическое округление, зажим в 0, fallback при отсутствии
      yandexPrice/manualPrice; 11 тестов (PRICE-01…09)
- [x] Модификаторы: ручная цена > общий коэффициент
- [x] recomputePrices: пересчёт Dish.price/Modifier.price в БД после sync,
      смены настроек, правки блюда — сервер заказа видит только рассчитанную цену
- [x] Админка: «Ценообразование (общее)» в настройках; в редакторе блюда —
      цена Яндекс, режим, ручная/%, итог
### Этап ТЗ-4 — Модификаторы/соусы [x]
- [x] validateModifierSelection (6 тестов): min/max, чужие ID, повторы,
      сверх лимита — единый модуль для UI и сервера
- [x] Модалка: max=1 → radio, max>1 → checkbox, «обязательно»/«до N»,
      блокировка добавления при нарушении min с понятной ошибкой
- [x] Сервер заказа: модификаторы только из блюда (чужие → 400),
      min/max по группам (нарушение → 400)
- [x] Обязательный тест: три соуса — 550+50+50+60=710 на сервере,
      4-й сверх max → 400, чужой ID → 400 (E2E, оба проекта)
- [x] Виден каждый вариант с ценой в корзине/заказе (снимок модификаторов)
### Этап ТЗ-5 — Доставка с интервалами [x]
- [x] Генератор окон `lib/delivery/slots.ts` (12 тестов): tz ресторана
      (Intl), minAhead, daysAhead, дни, часы, исключения, заказ на завтра
      ночью, выключенный вариант
- [x] DeliveryOption: поля ТЗ §9.1; seed создаёт дефолтный asap-вариант
      из зон (обратная совместимость)
- [x] /api/delivery/slots: варианты + окна с сервера
- [x] Заказ: валидация окна перед созданием (прошедший/несуществующий → 400),
      порог бесплатности обнуляет вариант; при включённых вариантах зоны
      не применяются — единое начисление без двойной платы
- [x] Снимок в заказе: deliveryMode/date/slot/tz/название варианта +
      человекочитаемый desiredTime (asap → «как можно скорее»)
- [x] UI корзины: выбор варианта, даты, интервала; fallback на зоны при
      отсутствии вариантов
- [x] Админка «Доставка»: CRUD вариантов (режим, дни, часы, слот, minAhead,
      глубина, цена, порог, исключения)
- [x] E2E: админ создаёт scheduled-вариант через UI → заказ с окном,
      stale → 400, порог → 0, asap — оба проекта
### Этап ТЗ-6 — Главная + банкеты [x]
- [x] Главная: hero («Заказать с доставкой» → #menu, «Банкеты» — скрыта при
      выключенном разделе/отсутствии залов) → полное меню (тот же MenuClient,
      без дубля) → акции/о нас/контакты ниже
- [x] /menu → redirect на /#menu (обратная совместимость ссылок)
- [x] Банкеты: BanquetHall (зал, вместимость, описание, фото, позиция) +
      настройки (заголовок, описание, условия, цены, контакт, CTA);
      /banquets 404 при выключенном разделе или отсутствии залов
- [x] Админка «Банкеты»: включение, настройки, CRUD залов с фото
- [x] E2E: UI-01…05 (структура главной, redirect, 404, включение через
      админку, кнопка); чистка данных за тестом
- [x] Устойчивость тестов: уникальные rate-limit бакеты/email по проекту
      (ранее флаки из 429 на общих бакетах)
### Этап ТЗ-7 — Фон [x]
- [x] Тема: background { enabled, image, position, dimPercent, disableOnMobile }
      (zod + ThemeConfig)
- [x] Upload: раздел background (2400px WebP), отказ SVG и >8 МБ;
      старый файл удаляется только после успешной записи нового состояния
- [x] Применение в layout: `html body` с затемнением, cover, позиция,
      media-запрос отключения на мобильных
- [x] Админка темы: включение, загрузка/замена/удаление, позиция,
      слайдер затемнения, мобильный флаг, превью
- [x] E2E VIS-01/02/03: загрузка, применение (с учётом мобильного флага),
      SVG → 400, удаление
- [x] Исправлены найденные баги: InstallPrompt показывался на /admin
      (перекрывал кнопку), sticky-кнопка сохранения перекрывалась на
      мобильном, `workers: 1` — параллельные проекты ломали общее
      состояние сайта в тестах (источник «флаки» всего набора)
### Этап ТЗ-8 — PWA-иконки и офлайн [x]
- [x] generateIcons (seed): 192/512/apple-touch 180 из логотипа с
      безопасными полями (90% холста); fallback платформы — accent-плашка
      с буквой названия (SVG→sharp), если логотипа нет
- [x] Версия иконок в icons/icons.json → ?v= в манифесте (cache-bust при
      смене логотипа); манифест теперь force-dynamic
- [x] apple-touch-icon в metadata layout с версией
- [x] Логотип в админке «Ресторан»: загрузка → иконки перегенерируются
- [x] Без офлайн-кэша меню; OfflineBanner «Нет подключения к сети»
- [x] E2E PWA-01/03/04/06: манифест с версией, fallback, скрытие кнопки,
      офлайн-плашка
### Этап ТЗ-9 — Регресс, сборка, доки [x]
- [x] Повторный регресс после волны D ч.3: 109 тестов Vitest шаблона,
      35 тестов платформы и 71 E2E-сценарий; чистый CI полностью зелёный.
- [x] ARCHITECTURE.md: новые модули и потоки (sync, цены, доставка,
      главная/банкеты, фон, PWA)
- [x] Миграция проверена на копии buxara_mig (см. ТЗ-1)
- [x] Итоговый отчёт — ниже

### Этап ПМ-1 — Визитки и магниты с QR-кодом [x]
- [x] Отдельный раздел `/admin/print-materials` и пункт «Печать» в админке.
- [x] Два независимых макета: визитка 90 × 50 мм и магнит 70 × 70 мм;
      вылеты 3 мм, безопасная зона 4 мм, направляющие только в preview.
- [x] Автоподстановка названия, логотипа, телефона, адреса и accent-цвета
      конкретного ресторана; fallback с первой буквой при отсутствии логотипа.
- [x] Три стиля, цвета, тексты и видимость контактов настраиваются отдельно
      для каждого формата; настройки сохраняются в Settings(printMaterials),
      миграция БД не нужна.
- [x] QR ведёт на canonical сайта и получает разные UTM-источники для визитки
      и магнита плюс настраиваемую campaign; URL нельзя подменить из браузера —
      canonical повторно устанавливается в server action и export API.
- [x] Защищённый admin export: SVG, PNG 300 dpi и PDF точного физического
      размера; серверная Zod-валидация, безопасное имя файла, QR level H,
      лимиты тела/rate/concurrency и защита чтения логотипа от symlink escape.
- [x] 16 unit/integration-тестов: размеры, URL/UTM, принудительный canonical,
      небезопасные протоколы, XML escaping, направляющие, реальный SVG/PNG/PDF
      и отказ на слишком большом запросе; lint, TypeScript и production build
      успешны.
- [x] Добавлен Playwright ПМ-01: вход в админку, preview, сохранение после
      reload, оба формата, неизменяемый canonical, реальное скачивание PNG и
      восстановление исходного текста; запускается в общем E2E-регрессе с
      одноразовой БД после merge.
- [x] Визуально проверены оба макета: QR, оффер и телефон не пересекаются и
      находятся внутри безопасной зоны при длинном тексте.
- Ограничение MVP: макеты односторонние и RGB. Перед большим тиражом нужен
  пробный экземпляр и подтверждение цветового профиля типографией.

## Отчёт по ТЗ (синхронизация и развитие витрины)

**Реализовано по этапам:**
1. Модели: source/externalId/цены-режимы/группы/часы/интервалы/фон/PWA —
   миграция аддитивная, проверена на копии, откат = дамп + схема из git-тега.
2. Синхронизация Яндекс.Еды: изолированный клиент, upsert по externalId,
   атомарный lock, retry, job + админка, 9 интеграционных тестов.
3. Цены: inherit/yandex/manual/coefficient, индивидуальный приоритет,
   округление, зажим в 0, коэффициент на модификаторы, 11 тестов.
4. Модификаторы: группы min/max, radio/checkbox, серверная валидация,
   обязательный тест «3 соуса = 710 ₽».
5. Доставка: окна с tz, minAhead/daysAhead/исключения, порог бесплатности,
   зоны заменяются вариантами (без двойного начисления), снимок в заказе,
   админка вариантов.
6. Главная с полным меню, /menu → /#menu; банкеты с 404 при выключенном.
7. Фон: загрузка (отказ SVG/>8МБ), затемнение, позиция, мобильный флаг.
8. PWA: иконки из логотипа с fallback, версия (cache-bust), офлайн-плашка.

**Тесты:** набор расширен до 135 Vitest шаблона + 35 Vitest платформы и
82 E2E Playwright (360 px Chromium, мобильный WebKit и desktop Chromium,
`workers: 1`). Полный набор пройден на dev-ВМ 2026-09-22 на одноразовых БД;
те же проверки контролирует обязательный GitHub Actions CI.

**Исправлено по ходу:** медленные картинки меню (прегенерированные 400/800px
вместо runtime-оптимизатора), InstallPrompt на /admin, sticky-кнопка на
мобильном, race-conditions в тестах (workers: 1, уникальные rate-limit
бакеты/email), cleanup sync-тестов затирал общую БД, fallback на полное фото
для старых наборов контента без карточного `-sm.webp`.

**Известные ограничения:** min/max групп из API Яндекс.Еды берутся как есть
(required/minSelected/maxSelected); у части блюд без externalId (старые
ручные) sync не сопоставляет (они и остаются ручными — by design);
офлайн-режим ограничен плашкой (без кэша — by design).

**Порядок развёртывания:** `git push` (есть) → на проде `update.sh` →
для каждого сайта: включить sync в админке (placeSlug) → проверить меню →
при желании настроить варианты доставки и фон.
- На dev-ВМ выполнено для buxara: sync включён (placeSlug `chajxana_buxara_xalyal`,
  interval 60 мин, cron */15 через `run-site-job.sh`), первая синхронизация
  успешна (130 upsert / 2 missing). Перед включением sync на сайтах, наполненных
  инжестом до ТЗ-1, обязателен `scripts/backfill-yandex-ids.sql` (иначе
  конфликт Dish_pkey: у старых блюд пустой externalId).
- Тарифная модель: фикс ₽/мес на сайт? От количества заказов? Пробный период?
  Цены и градации — решает владелец, в шаблоне: гибкая таблица тарифов.

## Новая общая админка ресторанов (dev-интеграция, 2026-09-25)

- [x] Первый локальный этап: новая оболочка в существующих защищённых
  `/admin` маршрутах. Название и логотип берутся из данных текущего сайта;
  ссылки на все прежние разделы сохранены, включая компактную панель на
  планшете и вручную свёрнутую панель на компьютере.
- [x] Списки и детали заказов и броней читаются из прежней Prisma-БД.
  Смена статусов вызывает существующие защищённые server actions; правила
  переходов и бизнес-расчёты на этом этапе не менялись.
- [x] История больше не обрезается до последних 50 заказов и 100 броней:
  сервер отдаёт по 25 записей, фильтры статуса и типа заказа, сортировка и
  счётчики считаются по всей БД. Переход между страницами и выбор карточки
  проверены на 360–900 px в локальном интерфейсе.
- [x] Локально введены отдельные цепочки статусов доставки и самовывоза:
  сервер разрешает только следующий этап, отмена остаётся отдельной;
  онлайн-оплата принимает новый заказ. Бонусы начисляются только на финале
  соответствующего типа. UI, кабинет, уведомления и фильтры используют
  общее определение кодов и подписей.
- [x] Подготовлена транзакционная миграция `0004_order_status_flow`:
  `delivering` → `handed_to_courier` (доставка) или `ready` (самовывоз),
  `done` → `delivered`/`issued` по типу; незнакомые данные вызывают откат.
- [x] На dev-ВМ (2026-09-25) миграции `0003` и `0004` применены к изолированным
  копиям БД `ochag-grill` и `buxara`. По 8 PostgreSQL-тестов на каждой копии
  прошли: перенос старых статусов, цепочка доставки с онлайн-оплатой,
  однократное начисление и возврат бонусов. После тестов хеши строк заказов,
  бонусов, меню, броней и постоянных настроек совпали с исходными БД.
  `syncState` исключён из сравнения: живой cron обновляет его после снимка.
- [x] На изолированных копиях всех трёх ресторанных БД («Очаг гриль», «Бухара»,
  «У мамы») применены все недостающие миграции до `0005_admin_users`.
  Хеши строк заказов (с ожидаемым преобразованием старых статусов), бонусов,
  меню, броней и постоянных настроек совпали с источниками; новые таблицы
  сотрудников и сессий пусты. Временные копии удалены, рабочие БД не менялись.
- [x] Минимальная локальная точка отката dev создана на самой ВМ:
  `/home/ilya/resto/rollback/admin-pr10-20260925` (каталог `700`, файлы `600`).
  В ней дампы БД `resto`, `buxara`, `ochag-grill` и архивы их `content/`,
  включая закрытый для пользователя `ilya` каталог `ochag-grill/content/icons`.
  Все шесть файлов прошли SHA-256; три дампа восстановлены в отдельные БД
  `admin_rehearsal_pr10_*`, а архивы content извлечены в отдельный каталог.
  Число строк и хеши 11 ключевых таблиц, а также записи миграций совпали с
  источниками; 42, 166 и 167 файлов content извлечены без ошибок.
  Рабочие БД и контейнеры не менялись; ничего не передавалось за пределы ВМ.
  Штатный `backup.sh` для этой точки не подходит: он включает только `sites/*`.
- [x] По решению пользователя отдельный повторный rehearsal на временных БД
  отменён. На dev остановлены запись двух целевых сайтов и их cron, после чего
  обновлена локальная точка отката `admin-pr10-live-cutover-20260925` с дампами
  БД и content. Архивы прочитаны и проверены по SHA-256. Из опубликованного
  commit `e2113bf` собраны отдельные образы; миграции до `0007` применены
  только к `buxara` и `ochag-grill`. Хеши и количество строк прежних таблиц
  совпали до/после, статусы заказов соответствуют ожидаемому преобразованию.
  Созданы и проверены отдельные учётки владельца и сотрудника в каждой БД.
  «У мамы» работает на прежнем образе и схеме. `update.sh` не применялся.
- [x] Live-проверка создала по одному тестовому заказу и брони на каждом сайте:
  суммы совпали с ценой блюда в БД; ненастроенная онлайн-оплата корректно
  отклонена. Браузер подтвердил вход владельца и сотрудника, права и маршруты.
  Найдено: прежняя запись `Settings` может не содержать `pricing`, из-за чего
  `/admin/settings` возвращал 500. Сначала добавлен падающий тест, затем
  чтение настроек дополнено значениями по умолчанию без изменения старой записи;
  тест и production build проходят локально.
- [x] Прямое открытие броней выявило различие серверного и браузерного часового
  пояса при показе времени создания. Для заказов и броней добавлен единый
  формат даты с часовым поясом ресторана; старым настройкам задано значение
  по умолчанию. Адресные тесты сначала падали, затем прошли вместе со сборкой.
- [x] На dev развернут закреплённый образ commit `634409a` только для «Бухары»
  и «Очага». В браузере на 360 px подтверждены вход владельца и сотрудника,
  права, заказы, брони, меню, настройки, ручная ссылка WhatsApp, API событий
  и звука: HTTP 200, без ошибок страницы и горизонтальной прокрутки. В каждой
  БД проверены тестовые заказ и бронь, корректные сумма, способ оплаты,
  отсутствие списания бонусов и два события. Онлайн-оплата на этих dev-сайтах
  не настроена и ожидаемо отклоняется. Четыре задания cron возвращены.
  «У мамы» и платформа доступны; PR № 10 остаётся открытым, `main` и prod
  не изменены. Для отката сохранены прежний образ и локальные дампы двух БД
  с content. Новая админка хранит пароли учётных записей в виде хешей;
  одноразовый файл для создания учёток удалён. Перед внешним доступом
  временный dev-пароль нужно сменить на сильный.
- [x] Адресная приёмка OTP: на каждом сайте тот же закреплённый образ с той же
  рабочей БД отправил письмо во временный SMTP-приёмник без внешнего доступа.
  Код из принятого MIME-письма подтвердился, гостевая сессия открыла кабинет;
  сессия одного тенанта не подошла другому. Приёмник и временные контейнеры
  удалены. Внешняя доставка этим тестом не подтверждена.
- [x] Обнаружено: на публичных dev-контейнерах `SMTP_URL` отсутствует, а
  Telegram, MAX и email-уведомления выключены. При отказе SMTP прежний код
  ошибочно сообщал гостю об отправке; добавлен адресный тест и исправление:
  без доставки API отвечает 503, а явный `AUTH_DEV_CODE=1` показывает код
  только в dev-ответе без записи кода или email в лог.
- [x] Исправление OTP из commit `022fe1d` развёрнуто только на двух dev-сайтах.
  Без SMTP публичный `/api/auth/request-code` проверенно возвращал 503 без
  кода или email в логах. Постоянный `AUTH_DEV_CODE=1` на публичных сайтах
  отклонён автоматической проверкой: он раскрыл бы гостевые коды в ответах.
  Обхода не было.
- [x] Подключён внутренний Mailpit: SMTP доступен контейнерам, web-интерфейс
  привязан только к loopback dev-ВМ, Caddy его не публикует. На обоих публичных
  сайтах подтверждены запрос OTP, письмо в Mailpit, успешная проверка кода и
  вход в кабинет; API код не возвращает. `AUTH_DEV_CODE` выключен.
- [x] Для каждого сайта на время теста включали только email-уведомления,
  создавали тестовые заказ и бронь и проверяли два письма в Mailpit. Затем
  флаг email возвращён в `false`. Адресные mock-тесты отправки уведомлений о
  заказе и брони прошли. Внешняя доставка не проверялась и сейчас каналы
  уведомлений выключены.
- [ ] Для prod заменить внутренний Mailpit на реальный `SMTP_URL`/`SMTP_FROM`,
  оставить `AUTH_DEV_CODE` выключенным, включить нужные каналы в настройках и
  проверить доставку на свой внешний адрес/чат. Онлайн-платёж потребует
  настройки провайдера; на dev выбран `none`. Кодовые сценарии dev приняты,
  внешние сервисы остаются конфигурационным условием переноса.
- [x] Issue № 7, локальный этап: в новой админке можно изменить количество,
  убрать позицию и добавить доступное блюдо из полного каталога с добавками.
  Существующие цены и добавки берутся из снимка заказа, новые — из текущей БД;
  сервер пересчитывает позиции, доставку по выбранной действующей зоне или
  прежнему варианту, лимит списания бонусов, кэшбэк и итог. Разницу списания
  фиксирует в ledger, а отмена возвращает его остаток. Изменение состава,
  суммы и ledger атомарно; блокировка строки заказа и проверка `updatedAt`
  отклоняют устаревшее редактирование. Для заказов с онлайн-оплатой,
  ненулевым статусом платежа, а также отменённых и завершённых заказов
  редактирование запрещено сервером и объяснено в UI. При увеличении количества
  недоступного блюда сервер отклоняет изменение. Старые заказы без сохранённой
  зоны требуют выбора действующей зоны для пересчёта доставки.
- [x] TDD этого этапа: новый интеграционный тест сначала падал на отсутствии
  сервиса; отдельный тест выявил неполный возврат бонусов при отмене после
  корректировки, ещё один — возможность увеличить недоступное блюдо.
  После исправлений полный прогон на одноразовой PostgreSQL: 207 тестов,
  29 файлов, все прошли. Локальные build, TypeScript и lint прошли (у lint
  остались 6 прежних предупреждений про `img`). Форму проверили в Chromium
  на 360, 390 и 768 px: нет горизонтальной прокрутки, кнопки ≥ 44 px.
- [x] Issue № 4, локальный этап: роли `owner` (все разделы и сотрудники) и
  `staff` (заказы и брони). Сотрудники хранятся отдельно в БД каждого сайта,
  имеют личный логин, имя и scrypt-хеш пароля с солью. Случайный токен сессии
  хранится в cookie, в БД — только его SHA-256-хеш; выход, смена пароля и
  отключение сотрудника отзывают сессии. Защита проверяет активность и роль
  на каждом запросе, серверные действия и прямые страницы. Владелец управляет
  учётными записями в `/admin/team`; имя вошедшего показано в шапке. Последнего
  активного владельца нельзя отключить. Старый общий пароль новая версия не
  принимает: перед переключением нужно мигрировать БД и создать первого
  владельца командой `scripts/bootstrap-admin.ts`, передав логин, имя и пароль
  через закрытый env-файл только на время запуска. Прежний код продолжает
  работать до контролируемого переключения; старые cookie после него невалидны.
- [x] На двух одноразовых PostgreSQL прошли 212 тестов, в том числе личный
  вход, ошибки пароля, права, отзыв сессий, запрет повторного bootstrap и
  изоляция сессии между двумя БД. Команда bootstrap проверена отдельно.
  Локальные страницы входа и управления сотрудниками проверены в Chromium на
  360, 390 и 768 px: горизонтальной прокрутки нет, зоны касания ≥ 44 px.
- [ ] Браузерный полный цикл «вход → создание сотрудника → вход сотрудника»
  проверить в изолированном окружении после контролируемого переключения.
  Туннель к общему PostgreSQL-порту dev-ВМ отклонён автоматической проверкой
  из-за доступа к другим БД; подготовленная одноразовая БД удалена.
- [x] Issue № 8, локальный этап: новые заказы с оплатой при получении и брони
  создают `AdminEvent` в транзакции записи; онлайн-заказ создаёт событие только
  при первом подтверждении оплаты. Админка опрашивает свою БД каждые 10 секунд,
  обновляет списки и показывает событие; `AdminEventReceipt` выдаёт его один раз
  каждому сотруднику даже при нескольких вкладках и после перезагрузки.
  Звук воспроизводится только в открытой админке после действия пользователя,
  разрешающего аудио в браузере. События старше пяти минут показываются без
  звука. Системные push-уведомления после закрытия вкладки не включены.
- [x] В настройках доступны ровно два встроенных сигнала и один загружаемый
  MP3/WAV/Ogg до 2 МБ. Тип и сигнатура проверяются на сервере, файл живёт в
  `CONTENT_DIR/audio/` текущего ресторана. Сигнал можно выбрать и прослушать;
  удаление своего файла сразу выбирает первый встроенный. Миграция `0006_admin_events`
  добавляет события и отметки выдачи, рабочие dev-БД не изменены.
- [x] Локальные unit-тесты правил события и звука, TypeScript и build прошли.
  Интерфейс с тестовыми ответами API проверен в Chromium на 360, 390 и 768 px:
  оба сигнала играют, событие появляется, свой звук загружается и удаляется,
  полная шапка и настройки не дают горизонтальной прокрутки; кнопки ≥ 44 px.
- [x] Issue № 9, локальный этап: оформление заказа предлагает гостю выбрать
  звонок, WhatsApp или Telegram и сохраняет выбор в `Order.preferredChannel`.
  Админка показывает только выбранный канал, без подмены при скрытии в
  настройках. Старые заказы без выбора и брони показывают оба разрешённых
  ручных канала. Номер нормализуется в международный формат; при ошибке ссылка
  не создаётся. `wa.me` открывается только по клику сотрудника без текста и
  автоматической отправки. Видимость каналов хранится в `Settings` сайта и
  сохраняется сразу при переключении. Миграция `0007_guest_contact` добавляет
  nullable поле без изменения старых заказов.
- [x] Адресные тесты выбора и ссылок прошли; карточки и оформление проверены
  в Chromium на 360, 390 и 768 px: ссылки и выбор доступны с зонами касания
  ≥ 44 px, горизонтальной прокрутки нет. Данные заказа и брони при переходе
  по ссылке не изменяются.
- [x] PR № 10: GitHub Actions применил и проверил миграции `0004`–`0007` на
  одноразовой PostgreSQL. Полный Vitest: 36 файлов, 226 тестов прошли,
  один тест изоляции между двумя БД пропущен (в CI поднята одна БД).
  Интеграционные тесты `admin-events-integration` и
  `guest-contact-integration` прошли. Создан одноразовый владелец;
  Playwright: 81 из 81 сценария на 360 px, WebKit mobile и desktop,
  включая подтверждение брони на общем экране. Job `platform` прошёл.
  Подтверждённый run: `36158277390`; рабочие БД, dev-контейнеры и prod
  не менялись.
- [x] Отдельная проверка миграций на копиях отменена пользователем перед
  контролируемым dev-переключением. CI проверил миграции и браузерные циклы
  на одноразовой БД; живые БД проверены по инвариантам после остановки записи.
  Автоматическая проверка ранее отклонила туннель и передачу архива на dev-ВМ;
  обхода не было.
- [x] Предсдаточная сверка с прототипом и issues № 3–9: на общем рабочем экране
  появились ближайшие новые и подтверждённые брони, подтверждение/отклонение
  новых броней и переход к
  полному списку; владельцу доступен быстрый переход к редактированию меню.
  Доставка и самовывоз отмечены разными цветами и текстом. Сервер меняет
  статус брони условным обновлением только из допустимого исходного состояния,
  поэтому устаревшая вкладка не может подтвердить обработанную бронь.
  Три адресных теста сначала падали на отсутствии сервиса, затем прошли;
  всего без PostgreSQL прошли 191 тест из 28 файлов. Локальные TypeScript,
  проверка Prisma-схемы, production build и `git diff --check` прошли;
  общий lint завершился без ошибок, с шестью прежними предупреждениями об `img`.
  Отслеживаемых секретных файлов и данных конкретного ресторана в коде не
  обнаружено. Живой браузерный цикл с новыми бронями ждёт одноразовую БД.
- [x] Локальный этап `codex/new-admin-dev`: рабочее меню в новой компоновке
  использует Prisma-категории и блюда, поиск, фильтр, редактирование, стоп-лист,
  добавление, удаление и загрузку фото через действующие server actions/API.
  Настройки сайта сгруппированы в карточки; остальные действующие разделы
  доступны из списка настроек в общей компоновке и сохраняют свои серверные
  действия. Локальная страница настроек проверена в Chromium на 360, 900 и
  1280 px без горизонтального переполнения (с fallback-контентом, без БД).
  Сохранение настроек и пересчёт цен теперь выполняются одной транзакцией;
  сервер проверяет границы сумм доставки, цен и бонусов до записи.
- Локальная сборка и проверка Prisma-схемы прошли. Полный тестовый прогон
  выполнен на одноразовой PostgreSQL на dev-ВМ; рабочие БД не изменялись.
  Развёртывание на dev — после завершения следующих этапов и контролируемого
  переключения.

### Перенос полного интерфейса прототипа в рабочую админку (2026-09-25)

Визуальное ТЗ — весь `test-front/`: оболочка, заказы, брони, меню, настройки,
вкладки, диалоги и состояния. Демо-данные и хранение настроек в localStorage
в рабочий сайт не переносятся; содержимое и действия берутся из действующих
серверных источников. Существующие ограничения статусов, ролей и серверный
пересчёт заказа остаются обязательными.

| Экран или состояние | Состояние переноса |
|---|---|
| Общая оболочка: бренд, навигация, профиль, тема, уведомления | Код первого dev-среза готов; визуальная приёмка впереди |
| Заказы: фильтры, сортировка, карточки, детали, состав, выбор блюда, статусы | Код первого dev-среза готов; визуальная приёмка впереди |
| Брони: список, деталь, подтверждение, отклонение | Опубликовано на dev и принято после визуальной проверки |
| Меню: категории, поиск, карточки, стоп-лист, редактор, фото, новое блюдо | Опубликовано на dev и принято после визуальной проверки |
| Настройки: оформление панели, звук, профиль, ресторан и часы, оформление сайта, доставка, цены, оплата, лояльность, связь, каналы, бронирование | Действующие разделы опубликованы на dev; визуальные расхождения форм исправляются перед повторной приёмкой |
| Дополнительные секции прототипа: акции, галерея, банкеты, страницы, синхронизация, печать, подписка | Действующие разделы опубликованы на dev; визуальные расхождения форм исправляются перед повторной приёмкой |
| Разделы старой рабочей админки без прямого экрана в прототипе: сотрудники, отдельная доставка, отдельные страницы контента, оплата подписки | Сохранить рабочие серверные действия и привести к общей дизайн-системе |
| Состояния: светлая и тёмная темы, пустые списки, ошибки, диалоги и мобильные 360/390/768 px | Проверить по каждому разделу |

Первый срез: стили и JSX оболочки/заказов перенесены из прототипа на реальные
данные; редактор сохраняет через `saveOrderItems`, предварительная сумма
блюд покрыта адресным тестом, итог пересчитывает сервер. Локальные TypeScript,
lint и production build прошли. Перед dev-публикацией остаются CI и
браузерная проверка на двух сайтах; затем работа продолжается по таблице.
Первый CI-прогон выявил потерю быстрых действий заказа и брони и узкое поле
количества; кнопки с серверными обработчиками возвращены, поле исправлено.
Повторная проверка CI и dev-публикация ещё не завершены.

CI первого среза выявил девять E2E-падений в трёх сценариях на мобильном и
настольном экранах. В отдельной ветке восстановления исправлены стартовое
состояние редактора заказа и зона клика на мобильной карточке, увеличена зона
профиля до 44 px. E2E брони и последовательной смены статусов адаптированы
к перенесённому интерфейсу с проверкой фактического результата в БД; проверка
размеров игнорирует невидимые поля формы. Результат повторного прогона CI
первого среза будет отмечен после проверки. Локально прошли production build,
lint без ошибок (шесть прежних предупреждений) и 28 адресных тестов. Полный
Vitest локально не завершился из-за недоступности PostgreSQL; браузерный E2E
требует отдельной базы, как в CI. Эти проверки не считаются пройденными до
повторного запуска в среде с базой.

Повторный CI на одноразовой PostgreSQL подтвердил миграции, все unit и
интеграционные тесты и работу платформы. Из 81 браузерного сценария прошли 75;
оставшиеся падения показали, что ссылку на деталь брони перекрывают быстрые
кнопки, а последняя проверка статуса заказа неоднозначно находила и метку, и
пункт списка. Ссылка брони теперь занимает собственную область рядом с
быстрыми действиями; проверка статуса адресует видимую метку. Повторный CI
`36176298396` прошёл: 41 файл Vitest с PostgreSQL, 81 из 81 браузерных
сценариев, сборка и проверка платформы. Первый визуальный срез готов к
отдельной приёмке и dev-публикации; dev этим коммитом не менялся.

Первый визуальный срез PR № 10 опубликован на dev из проверенного коммита
`1f5d932` только для «Очага» и «Бухары»: закреплённый образ
`resto-template:admin-pr10-1f5d932` запущен в обоих app-контейнерах.
Публичные `/` и `/admin/login` обоих сайтов и localhost-прототип отвечают
HTTP 200. Рабочий checkout `main`, платформа, демо «У мамы» и базы не
переключались. Сохранены прежний образ и копии compose override для отката.
Проверка всех экранов и визуального соответствия прототипу остаётся открытой.

Независимая визуальная приёмка первого dev-среза выявила сжатые три колонки
«Заказов» на ширине 1280 px. Раскладка в коде приведена к прототипу: до
1500 px список заказов и детали стоят рядом, а блок броней переносится ниже;
при нехватке места панели выстраиваются в одну колонку. В узкой колонке
карточка заказа раскладывается на две строки. Браузерный сценарий проверяет
геометрию на 1280 px и отсутствие переполнения на 960 px. Развёртывание на
двух dev-сайтах выполнено, повторная визуальная проверка впереди. Расхождения
страниц «Брони», «Меню», «Настройки», бейджа уведомлений и логотипа «Бухары»
остаются в очереди; перенос всех страниц ещё не завершён.

CI `36183938554` прошёл: template 84/84 Playwright, platform 4/4, обе задачи
успешны. Коммит `ca98936` собран на dev в закреплённый образ
`resto-template:admin-pr10-ca98936`; обновлены только app-контейнеры «Очага»
и «Бухары». Их публичные страницы и `/admin/login` отвечают HTTP 200, исходный
прототип на localhost:3097 — HTTP 200. Dev checkout `main` и образы платформы
и «У мамы» остались прежними. Визуальная приёмка этой раскладки ещё открыта.

Повторная визуальная приёмка подтвердила две колонки «Заказов» на 1280 px, но
обнаружила сломанную тёмную тему: позднее правило `.shell` перекрывало общие
цветовые токены `.dark`. Селектор тёмной темы усилен до `.shell.dark`, чтобы
фон, текст и панели переключались вместе. Добавлен E2E на цвета и сохранение
темы после перезагрузки. CI шаблона и платформы прошёл, исправление
опубликовано на двух dev-сайтах. Повторная независимая визуальная проверка
подтвердила читаемость тёмной темы, обратное переключение и сохранение выбора.

Страница «Брони» в коде перестроена по образцу прототипа: вводный заголовок,
блок предстоящих броней и детали справа; на узком экране панели идут одна под
другой. Для предстоящих данных используется существующая серверная выборка,
подтверждение и отклонение остаются серверными действиями. Список всех броней,
фильтры, сортировка и пагинация сохранены в раскрываемом блоке для истории.
Первый CI показал, что отдельный браузерный тест добавил лишний вход и достиг
общего лимита входов E2E. Проверка раскладки перенесена в уже авторизованный
сценарий. Повторный CI прошёл, раздел опубликован на обоих dev-сайтах.
Независимая визуальная приёмка подтвердила композицию и работу обеих тем, но
нашла расхождения в ширине страницы, шрифтах двух заголовков и расположении
ссылок для связи. Код исправлен по прототипу, повторный CI и dev-проверка прошли.

Если в данных ресторана указан путь к отсутствующему логотипу, боковая панель
админки показывает первую букву названия вместо сломанного изображения.
Повторная приёмка «Броней» подтвердила исправление ширины, заголовков и ссылок.
На мобильном экране обнаружены скрытые содержимое переключателя темы и счётчики
в нижней навигации; оба элемента восстановлены, опубликованы на dev и приняты
после независимой проверки. Fallback логотипа «Бухары» также принят.

Экран «Меню» в коде использует структуру прототипа: категории переносятся по
строкам, карточки показывают фото, название, описание, вес, цену и наличие.
Редактирование и добавление блюд оформлены в тех же карточках и формах; реальные
данные, загрузка фото, стоп-лист и серверные действия сохранены. Добавлены
браузерные проверки структуры, мобильной ширины и тёмной темы. Первый вариант
прошёл CI и опубликован для двух ресторанов на dev. Визуальная сверка выявила
узкое поле поиска и неверный шрифт названий блюд; оба отличия исправлены в
коде. Поле цены теперь видно в редакторе при любом режиме, а его изменение
переключает блюдо на ручную цену. Повторные CI и dev-проверка прошли,
независимая визуальная приёмка подтвердила экран на 1280 и 360 px в обеих темах.

«Настройки» перестроены как вертикальный набор карточек прототипа: внешний вид
панели, звук, профиль, ресторан, оформление сайта, доставка, цены и лояльность,
связь, уведомления, бронь, акции, галерея, банкеты, страницы, синхронизация,
печатные материалы и подписка. Карточки используют действующие серверные
формы и данные текущего ресторана. Для основного блока параметры сохраняются
по разделам; изменение цен и лояльности по-прежнему идёт через транзакционное
действие с серверным пересчётом. Отдельные маршруты подразделов сохранены и
приведены к общей композиции. Добавлена браузерная проверка мобильной ширины
и наличия разделов; CI, dev-публикация и визуальная приёмка впереди.
Первый CI-прогон выявил переполнение мобильной строки часов работы и старый
браузерный сценарий с общей кнопкой сохранения. Строка переведена на две
колонки при 360 px, сценарий обновлён под сохранение карточки оформления.
Следующий CI-прогон прошёл, настройки опубликованы на двух dev-сайтах. Независимая
проверка нашла отличия от прототипа: ширину полей и строк часов, оформление
логотипа, расположение переключателей и кнопок, плотность форм нижних карточек.
Формы приводятся к двухколоночной сетке прототипа без замены серверных действий;
повторная визуальная приёмка ещё не завершена.
После замечаний по нижним карточкам редактор страниц получил горизонтальные
вкладки, поля в две колонки и локальный предпросмотр черновика. Редактор
печатных материалов сохраняет расширенные функции, но использует компактные
вкладки, сетку основных полей и кнопки по визуальному шаблону прототипа;
добавление зала сделано широким контурным действием.
Первый CI-прогон этого визуального исправления обнаружил зависимость E2E
сценария доставки от прежнего имени кнопки добавления варианта; имя возвращено,
оформление кнопки сохранено по шаблону прототипа. Финальный CI обязателен перед
следующей dev-публикацией.
Повторный прогон выявил такую же зависимость сценария банкета от подписи
кнопки «+ Зал»; подпись восстановлена, широкая контурная кнопка оставлена.
